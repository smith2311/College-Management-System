<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:title="Manage Attendance"
            android:id="@+id/manage_attendance"
            />
        <item android:title="Manage Internal Marks"
            android:id="@+id/manage_internal_marks"
            />
        <item android:title="Manage Assignment Marks"
            android:id="@+id/manage_assignment_marks"
            />
        <item android:title="Change Password"
            android:id="@+id/change_pwd"
            />

        <item android:title="Logout"
            android:id="@+id/logout"
            />
    </group>
</menu>